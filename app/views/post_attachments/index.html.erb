<p id="notice"><%= notice %></p>

<h1>مرفقات المنشور</h1>

<table>
  <thead>
    <tr>
      <th>المقالة</th>
      <th>اﻻسم</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @post_attachments.each do |post_attachment| %>
      <tr>
        <td><%= post_attachment.paper_id %></td>
        <td><%= post_attachment.fname %></td>
        <td><%= link_to 'عرض', post_attachment %></td>
        <td><%= link_to 'تعديل', edit_post_attachment_path(post_attachment) %></td>
        <td><%= link_to 'حذف', post_attachment, method: :delete, data: { confirm: 'هل أنت متأكد?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'مرفق جديد', new_post_attachment_path %>
